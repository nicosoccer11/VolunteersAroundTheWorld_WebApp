<!DOCTYPE html>
<html>
<head>
  <title>Your Page Title</title>
  <style>
    body {
      background-color: #f1efee;
      margin: 0;
      padding: 0;
    }

    .container {
      background-color: white;
      padding: 20px;
      border-radius: 10px;
      margin: 20px;
    }

    h1 {
      margin: 20px 0;
    }

    ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    li {
      margin-bottom: 10px;
      display: flex;
      align-items: center;
      border: 1px solid #ccc;
      padding: 10px;
    }

    .event-name {
      flex: 1;
    }

    .event-date {
      margin-left: 10px;
    }

    /* .simple-calendar styles */
    .simple-calendar {
      /* CSS styles for simple_calendar go here */
      background-color: #eee;
      border: 1px solid #ccc;
      padding: 5px;
      display: flex;
      flex-wrap: wrap; /* Wrap elements to the next line if necessary */
      justify-content: space-between; /* Space the days evenly */
      margin: 5px;
    }

    .simple-calendar .day {
      flex: 1; /* Each day takes equal width to span the full container */
      min-width: calc(100% / 7); /* Minimum width for each day (for a week) */
      box-sizing: border-box; /* Ensure padding and border don't affect width */
      text-align: center; /* Center align the content */
    }

    .simple-calendar .wday-0 {}
    .simple-calendar .wday-1 {}
    .simple-calendar .wday-2 {}
    .simple-calendar .wday-3 {}
    .simple-calendar .wday-4 {}
    .simple-calendar .wday-5 {}
    .simple-calendar .wday-6 {}

    .simple-calendar .today {}
    .simple-calendar .past {}
    .simple-calendar .future {}

    .simple-calendar .start-date {}

    .simple-calendar .prev-month {}
    .simple-calendar .next-month {}
    .simple-calendar .current-month {}

    .simple-calendar .has-events {}

    /* Calendar navigation buttons */
    .calendar-nav {
      width: 100%; /* Take the full width */
      padding: 5px; /* Add padding for spacing */
      box-sizing: border-box; /* Ensure padding doesn't affect width */
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>All Events</h1>
    <ul>
      <% @events.each do |event| %>
        <li>
          <span class="event-name"><%= event.name %></span>
          <span class="event-date"><%= event.date %></span>
        </li>
      <% end %>
    </ul>
    <!-- Calendar navigation buttons -->
    <div class="calendar-nav">
      <%= month_calendar events: @events do |date, events| %>
        <div class="simple-calendar">
          <%= date %>
          <% events.each do |event| %>
            <div>
              <%= event.name %>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
    <div class="back-link">
      <%= link_to "Back to Admin Dashboard", admin_dashboard_path %>
    </div>
  </div>
</body>
</html>
