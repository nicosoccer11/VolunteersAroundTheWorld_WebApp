<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      background-color: #f1efee;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh; 
    }

    .container {
      background-color: white;
      padding: 20px;
      border-radius: 10px;
      margin: 20px;
      width: 80%; 
      max-width: 600px; 
    }

    h1, form {
      margin-bottom: 20px; 
    }

    .field {
      margin-bottom: 10px; 
      margin-right: 10px;
    }

    label {
      display: block; 
    }

    input[type="text"],
    input[type="date"] {
      width: 100%; 
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    .actions {
      text-align: center;
    }

    .submit-button {
      background-color: green;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    .back-link {
      background-color: #5a9bec;
      color: white;
      padding: 10px 15px; /* Padding inside the buttons */
      border-radius: 5px; /* Rounded corners for buttons */
      text-decoration: none; /* Remove underline from links */
      display: inline-block; /* Align buttons side by side */
      margin-top: 20px; 
      text-align: center;
      cursor: pointer;
      display: block; 
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Create a Final Countdown Event</h1>

    <%= form_with(model: @event, url: create_final_countdown_event_path, method: :post) do |form| %>
      <div class="field">
        <%= form.label :name %>
        <%= form.text_field :name %>
      </div>

      <div class="field">
        <%= form.label :date %>
        <%= form.date_field :date %>
      </div>

      <div class="actions">
        <%= form.submit "Create Final Countdown Event", class: "submit-button" %>
      </div>
    <% end %>

    <div class="back-link">
      <%= link_to "Back to Admin Dashboard", admin_dashboard_path %>
    </div>
  </div>
</body>
</html>


